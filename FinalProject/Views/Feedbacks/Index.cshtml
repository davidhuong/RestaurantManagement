@model IEnumerable<FinalProject.Models.tblGuestFeedback>

@{
    ViewBag.Title = "Index";
}

<h2>Feedback from Guests</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.feedback_content)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.date_submitted)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.guest_email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblGuest.guest_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.feedback_content)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.date_submitted)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.guest_email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblGuest.guest_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.feedback_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.feedback_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.feedback_id })
        </td>
    </tr>
}

</table>
